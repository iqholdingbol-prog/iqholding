# Checkpoint de seguridad — IQG-001.2

## Branch actual

`feature/iqg-001-2-runtime-remediation`

## Commit de checkpoint

`e4cc867` — `IQG-001.2: checkpoint de seguridad DDL y ADR`

## Archivos modificados

Incluidos en `e4cc867`:

- `schemas/core_schema.sql`
- `docs/adr/ADR-0001-bootstrap-y-contrato-de-acceso-sql.md`
- `docs/adr/ADR-0002-unicidad-pii-cifrada-y-referencias-externas.md`
- `docs/adr/ADR-0003-alcance-de-anonimizacion-y-retencion.md`

Locales y no commiteados, preservados para reanudar:

- `tests/pg16/README.md`
- `tests/pg16/sql/00_test_support.sql`
- `tests/pg16/sql/01_bootstrap_positive.sql`
- `tests/pg16/sql/02_fixture.sql`
- `tests/pg16/sql/concurrency/advisory_lock_a.sql`
- `tests/pg16/sql/concurrency/advisory_lock_b.sql`
- `tests/pg16/sql/concurrency/deadlock_a.sql`
- `tests/pg16/sql/concurrency/deadlock_b.sql`
- `tests/pg16/sql/concurrency/payment_reversal_a.sql`
- `tests/pg16/sql/concurrency/payment_reversal_b.sql`
- `tests/pg16/sql/concurrency/price_successor_a.sql`
- `tests/pg16/sql/concurrency/price_successor_b.sql`
- `tests/pg16/sql/concurrency/provision_same_key.sql`
- `tests/pg16/sql/negative/app_direct_access.sql`
- `tests/pg16/sql/negative/bad_operacion_line_unit.sql`
- `tests/pg16/sql/negative/bootstrap_direct_table.sql`
- `tests/pg16/sql/negative/cross_scope_write.sql`
- `tests/pg16/sql/negative/gateway_direct_access.sql`
- `tests/pg16/sql/negative/invalid_factura_without_lines.sql`
- `tests/pg16/sql/negative/untrusted_bootstrap_guc.sql`

Locales ajenos al ticket y excluidos sin inspección ni staging:

- `Documentos Legales/IQHOLDING_Dos_Opciones_Familia.docx`
- `Documentos Legales/IQHOLDING_Dos_Opciones_Familia.pdf`
- `legacy/vansam-v11/vansam-pos-v11.html`

## Cambios ya completados

- Capacidad de bootstrap separada mediante `iqg_bootstrap_invoker`; el control de bootstrap usa `session_user` y membresía de clúster, por lo que los GUC `iqg.*` por sí solos no autorizan la operación.
- ACL de mínimo privilegio: `iqg_app` e `iqg_gateway` no reciben privilegios SQL directos; la capacidad de bootstrap recibe únicamente el `USAGE` operativo y la firma revisada de `provisionar_empresa`.
- Reglas RLS y funciones de evidencia de contexto para exigir empresa, usuario, sucursal y membresía activos, con políticas específicas para sus tablas de evidencia.
- Validación de dominio que omite la validación y el bloqueo cuando el código de dominio no cambió.
- ADR de contrato bootstrap/pasarela, ADR de unicidad de PII cifrada y ADR de alcance de anonimización y retención.

## Cambios parcialmente completados

- Arnés PostgreSQL 16: hay README, soporte QA, bootstrap positivo, fixture 2 empresas por 2 sucursales y casos negativos/concurrentes iniciales.
- El arnés no tiene todavía `tests/pg16/run.ps1`, workflow `pg16-harness.yml`, secuencia de ejecución ni aserciones completas A–W. El README referencia el corredor inexistente; por eso todo `tests/pg16/` queda deliberadamente sin commit hasta completarlo y revisarlo como una unidad.

## Pruebas ejecutadas

- `git diff --check` del DDL antes del staging y `git diff --cached --check` del checkpoint, sin errores después de retirar espacios finales de los ADR.
- Revisión estática del DDL: delimitadores `$$` pares; un único `BEGIN;` y un único `COMMIT;`; presencia de capacidad bootstrap, controles de empresa/usuario activos y ocho referencias a `FORCE ROW LEVEL SECURITY`.
- Búsqueda estática de secretos en el DDL, ADR y arnés parcial: no se detectaron credenciales, llaves privadas ni tokens; las coincidencias son nombres de dominio/comentarios de diseño.
- Búsqueda estática de concesiones: no hay `GRANT` directo a `iqg_app` ni `iqg_gateway`.

## Pruebas aún NO ejecutadas

- Instalación y ejecución real en PostgreSQL 16 efímero.
- Matriz A–W, incluidas pruebas positivas y negativas de bootstrap, RLS, contexto, membresía activa, dominio, unidad de medida, fiscal, concurrencia, dump/restore y reejecución.
- Validación sintáctica/semántica por `psql` de la firma `GRANT EXECUTE` y de las políticas nuevas.
- Ejecución CI en GitHub Actions y evidencia de sus logs.

## Blockers

`CHECKPOINT_SAFE`. No hay bloqueo para preservar el trabajo ya consistente. El único bloqueo de terminación es deliberado: el arnés PG16 está incompleto y no se ejecutó ninguna prueba pesada por esta instrucción de checkpoint. `CEO_ACTION_REQUIRED=false`.

## Siguiente instrucción exacta para reanudar cuando vuelva la cuota

"En `feature/iqg-001-2-runtime-remediation`, completa y revisa como una sola unidad `tests/pg16/run.ps1`, la matriz A–W y `.github/workflows/pg16-harness.yml`; después ejecuta el arnés contra PostgreSQL 16 efímero, corrige únicamente fallos demostrados, crea un informe de evidencia nuevo, actualiza los documentos de estado y abre el PR hacia `master`. No modifiques `legacy/` ni `Documentos Legales/`."

## Cambios locales no commiteados

Sí. Permanece sin commit el directorio parcial `tests/pg16/` por carecer de corredor, workflow y matriz completa. También permanecen sin tocar y sin staging los tres archivos ajenos enumerados arriba. Este informe se commitea separadamente para que el checkpoint y su estado de reanudación queden preservados.
